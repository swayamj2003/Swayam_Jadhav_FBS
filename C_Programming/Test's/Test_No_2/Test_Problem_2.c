//Q.2 WAP to chack if num is positive, negative and neutral (0);

void main()
{
	int num = 06;
	
	if (num > 0)
	{
		printf("The num is positive = %d\n",num);
	}
	else if(num < 0)
	{
		printf("The num is negative = %d\n",num);
	}
	else
	{
		printf("The num is neutral = %d\n",num);
	}
}